#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false);cin.tie(0);
#define ll long long


int main() {
    fastio;
    int test;
    cin >> test;
    while(test--)
    {
        
	int arr[4];
	
	for(int i =0; i<4; i++){
	    cin>>arr[i];
	}
	
    float BOB = float(arr[0])/float(arr[1]);
    float Alice = float(arr[2])/float(arr[3]);
    
    if (BOB < Alice){
        cout<<"Bob"<<endl;
    }else if(BOB == Alice){
        cout<<"Equal"<<endl;
    }else{
        cout<<"Alice"<<endl;
    }
	
    }
	return 0;
}
