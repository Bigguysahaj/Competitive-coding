#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false);cin.tie(0);
#define ll long long

// void gcd(int a, int b){
//     if a == b
//         return a;
//     if (a >b)
//         gcd(a-b,b);
//     else
//         gcd(a,b-a);
// }

int main() {
    fastio;
    int test;
    cin >> test;
    while(test--)
    {
        
	int n,a,b;
	cin>>n;
	int arr[n];
	
	for(int i=0;i<n;i++){
	    cin>>arr[i];
	}
	
	sort(arr, arr +n);
	for (int i=0; i<n; i++){
	    cont = arr[i]
	    if arr[i] 
	}
	
	
    }
	return 0;
}
